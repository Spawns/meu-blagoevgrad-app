%ul
- if instance_variable_get("@#{type}_organizers").empty?
  %h5="Your have no #{type}s."
- else
  - instance_variable_get("@#{type}_organizers").each do |organizer|
    = render 'admin/organizers/organizer_attributes' , organizer: organizer
