= simple_form_for [:admin, @document] , :html => { :multipart => true }, :remote => "true"  do |f|
  %h4 New document
  
  = f.input :document, as: :file
  = f.input :name, as: :string
  = f.input :description, as: :text

  =f.button :submit ,  "Add document" , :class => "button expand"